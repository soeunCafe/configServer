server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/soeunCafe/configrepo.git
#          private ?? ??? ?? ? passwordname ????
